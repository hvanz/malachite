global:
  scrape_interval: 1s
scrape_configs:
{{#each nodes}}
  - job_name: "{{this.name}}"
    metrics_path: "/metrics"
    scrape_interval: 1s
    static_configs:
      - targets: ['host.docker.internal:{{this.host_metrics_port}}']
        labels:
          client_name: "malachite"
{{/each}}
